{
  "devDependencies": {
    "tailwindcss": "^4.0.14"
  },
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/output.css --watch",
    "build": "rm -rf dist; mkdir dist; cp src/* dist/; rm dist/input.css; npx @tailwindcss/cli -i ./src/input.css -o ./dist/output.css --minify"
  }
}
